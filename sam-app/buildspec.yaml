version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14.x
  build:
    commands:
      - sam package --template-file template.yaml --s3-bucket sam-practice-backet --output-template-file packaged-template.yaml
artifacts:
  files:
    - packaged-template.yaml
